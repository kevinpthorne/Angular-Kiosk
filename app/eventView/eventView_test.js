'use strict';

describe('myApp.eventView module', function() {

  beforeEach(module('myApp.eventView'));

  describe('eventView controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('TomorrowViewCtrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});